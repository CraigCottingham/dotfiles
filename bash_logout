#!/bin/bash
# ~/.bash_logout: executed when a user exits from an interactive login shell.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples

shell_session_update
